# Configuración del Servidor
PORT=3000
NODE_ENV=development

# Base de Datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=restaurant_db
DB_USER=root
DB_PASSWORD=

# JWT Secret
JWT_SECRET=tu_clave_secreta_muy_segura_cambiala_en_produccion
JWT_EXPIRES_IN=24h

# Socket.io
SOCKET_CORS_ORIGIN=http://localhost:5173

# Impresora Térmica
PRINTER_TYPE=epson
PRINTER_INTERFACE=tcp
PRINTER_IP=192.168.1.100
PRINTER_PORT=9100
# Para USB usar: PRINTER_INTERFACE=usb y especificar PRINTER_VENDOR_ID y PRINTER_PRODUCT_ID

# Uploads
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880
